{% extends 'base.html.twig' %}

{% block title %}Categoriy{% endblock %}

{% block body %}
    <h1>Categoriy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoriy.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ categoriy.title }}</td>
            </tr>
        </tbody>
    </table>

    <a class= "btn btn-primary" href="{{ path('app_categoriy_index') }}">back to list</a>

    <a class= "btn btn-warning" href="{{ path('app_categoriy_edit', {'id': categoriy.id}) }}">edit</a>

    {{ include('categoriy/_delete_form.html.twig') }}
{% endblock %}
